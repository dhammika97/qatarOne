<h1>Category<a href="#/addUser" class="btn btn-primary pull-right">Add New</a></h1>
<ol class="breadcrumb">
  <li class="active"> Category	{{ test }}</li>
</ol>
<table class="table table-hover tablesorter">
    <thead>
      <tr>
        <th>Category Name</th>
        <th>Category Entered Date </th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <tr ng-repeat="user in users.users">
      <td></td>
    <!--    <td>{{user.user_username}}</td>
        <td>{{user.user_email}}</td>
        
        <td><button class="btn btn-primary">Revoke Access Token</button> <a href="#/editUser/{{user.user_id}}" class="btn btn-warning">Edit</a> <button class="btn btn-danger" ng-click="deleteUser(user.user_id)">Delete</button></td>-->
      </tr>
    </tbody>
</table>
